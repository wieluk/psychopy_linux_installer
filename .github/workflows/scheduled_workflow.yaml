name: weekly scheduled workflow

on:
  schedule:
    - cron: '0 8 * * THU'
  
jobs:
  # Test installation
  test-installation-quick:
    needs: syntax-check
    uses: ./.github/workflows/reusable_test.yaml
    with:
      test-mode: "quick"